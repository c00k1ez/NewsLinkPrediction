experiment_name: softmax_loss_baseline_bert

backbone_model: BertModel
freeze_backbone: true
main_model: BaselineSiameseNetwork
cos_margin: 0.25

model:
  encoder_hidden: 768
  output_dim: 256

criterion_name: SoftmaxLoss

optimizer:
  lr: 5e-5

#scheduler:
#  num_warmup_steps: 200

trainer:
  max_epochs: 6

loaders:
  batch_size: 100 # for 16gb Tesla P100
#  num_workers: 4